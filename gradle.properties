# Shared by the Flyway Gradle plugin (declared in the version catalog in settings.gradle.kts) and
# the flyway-database-postgresql module on the buildscript classpath in build.gradle.kts. Both must
# be the exact same version, so they read it from here.
flywayVersion=11.14.0
postgresqlVersion=42.7.8
